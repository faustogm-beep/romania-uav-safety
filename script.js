const I18N = {
    en: {
        status_ok: "Clear to Fly",
        status_ok_sub: "Maintain visual line of sight.",
        status_warn: "Caution Required",
        status_warn_sub: "Active NOTAMs nearby.",
        status_rest: "Restricted",
        status_rest_sub: "Check permits required.",
        status_loading: "Analyzing...",
        search_placeholder: "Search location or zone...",
        checklist_title: "Safety Checklist",
        disclaimer_short: "For info only. Check ROMATSA.",
        checklist: ["ID visible", "Battery charged", "Props checked", "Signal tested", "Airspace verified", "Privacy respected"],
        details_limit_low: "Lower Limit",
        details_limit_up: "Upper Limit",
        details_status: "Status",
        details_contact: "Contact",
        details_notam_from: "From (UTC)",
        details_notam_to: "To (UTC)",
        details_message: "Message"
    },
    es: {
        status_ok: "Libre para Volar",
        status_ok_sub: "Mantén contacto visual.",
        status_warn: "Precaución",
        status_warn_sub: "NOTAMs activos cerca.",
        status_rest: "Restringido",
        status_rest_sub: "Verifica permisos.",
        status_loading: "Analizando...",
        search_placeholder: "Buscar lugar o zona...",
        checklist_title: "Lista de Seguridad",
        disclaimer_short: "Solo informativo. Consulta ROMATSA.",
        checklist: ["ID visible", "Batería cargada", "Hélices OK", "Señal probada", "Espacio verificado", "Privacidad OK"],
        details_limit_low: "Límite Inferior",
        details_limit_up: "Límite Superior",
        details_status: "Estado",
        details_contact: "Contacto",
        details_notam_from: "Desde (UTC)",
        details_notam_to: "Hasta (UTC)",
        details_message: "Mensaje"
    },
    ro: {
        status_ok: "Liber pentru Zbor",
        status_ok_sub: "Mențineți contactul vizual.",
        status_warn: "Atenție Necesară",
        status_warn_sub: "NOTAM-uri active în apropiere.",
        status_rest: "Zbor Restricționat",
        status_rest_sub: "Verificați permisele.",
        status_loading: "Se analizează...",
        search_placeholder: "Caută locație sau zonă...",
        checklist_title: "Lista de Verificare",
        disclaimer_short: "Doar informativ. Consultați ROMATSA.",
        checklist: ["ID vizibil", "Baterie încărcată", "Elice verificate", "Semnal testat", "Spațiu aerian verificat", "Confidențialitate"],
        details_limit_low: "Limita Inferioară",
        details_limit_up: "Limita Superioară",
        details_status: "Status",
        details_contact: "Contact",
        details_notam_from: "De la (UTC)",
        details_notam_to: "Până la (UTC)",
        details_message: "Mesaj"
    },
    it: {
        status_ok: "Libero al Volo",
        status_ok_sub: "Mantieni il contatto visivo.",
        status_warn: "Attenzione",
        status_warn_sub: "NOTAM attivi nelle vicinanze.",
        status_rest: "Limitato",
        status_rest_sub: "Verifica i permessi.",
        status_loading: "Analisi in corso...",
        search_placeholder: "Cerca luogo o zona...",
        checklist_title: "Checklist Sicurezza",
        disclaimer_short: "Solo informativo. Consulta ROMATSA.",
        checklist: ["ID visibile", "Batteria carica", "Eliche controllate", "Segnale testato", "Spazio verificato", "Privacy"],
        details_limit_low: "Limite Inferiore",
        details_limit_up: "Limite Superior",
        details_status: "Stato",
        details_contact: "Contatto",
        details_notam_from: "Dal (UTC)",
        details_notam_to: "Al (UTC)",
        details_message: "Messaggio"
    },
    fr: {
        status_ok: "Libre de Voler",
        status_ok_sub: "Gardez le contact visuel.",
        status_warn: "Attention Requise",
        status_warn_sub: "NOTAM actifs à proximité.",
        status_rest: "Vols Limités",
        status_rest_sub: "Vérifiez les permis.",
        status_loading: "Analyse...",
        search_placeholder: "Chercher lieu ou zone...",
        checklist_title: "Liste de Sécurité",
        disclaimer_short: "Info seulement. Consultez ROMATSA.",
        checklist: ["ID visible", "Batterie chargée", "Hélices vérifiées", "Signal testé", "Espace vérifié", "Vie privée"],
        details_limit_low: "Limite Inférieure",
        details_limit_up: "Limite Supérieure",
        details_status: "Statut",
        details_contact: "Contact",
        details_notam_from: "De (UTC)",
        details_notam_to: "À (UTC)",
        details_message: "Message"
    },
    de: {
        status_ok: "Flug Erlaubt",
        status_ok_sub: "Sichtverbindung halten.",
        status_warn: "Vorsicht Geboten",
        status_warn_sub: "Aktive NOTAMs in der Nähe.",
        status_rest: "Eingeschränkt",
        status_rest_sub: "Genehmigungen prüfen.",
        status_loading: "Analyse...",
        search_placeholder: "Ort oder Zone suchen...",
        checklist_title: "Sicherheits-Check",
        disclaimer_short: "Nur Info. ROMATSA prüfen.",
        checklist: ["ID sichtbar", "Akku voll", "Propeller OK", "Signal getestet", "Luftraum geprüft", "Privatsphäre"],
        details_limit_low: "Untergrenze",
        details_limit_up: "Obergrenze",
        details_status: "Status",
        details_contact: "Kontakt",
        details_notam_from: "Von (UTC)",
        details_notam_to: "Bis (UTC)",
        details_message: "Meldung"
    },
    ast: {
        status_ok: "Llibre pa Volar",
        status_ok_sub: "Caltén contautu visual.",
        status_warn: "Precuru",
        status_warn_sub: "NOTAMs activos cerca.",
        status_rest: "Limitáu",
        status_rest_sub: "Mira los permisos.",
        status_loading: "Analizando...",
        search_placeholder: "Busca sitiu o zona...",
        checklist_title: "Llista de Seguridá",
        disclaimer_short: "Solo info. Consulta ROMATSA.",
        checklist: ["ID visible", "Batería cargada", "Hélices OK", "Señal probada", "Espaciu verificáu", "Privacidá OK"],
        details_limit_low: "Llímite Inferior",
        details_limit_up: "Llímite Superior",
        details_status: "Estáu",
        details_contact: "Contautu",
        details_notam_from: "Dende (UTC)",
        details_notam_to: "Hasta (UTC)",
        details_message: "Mensaxe"
    },
    cat: {
        status_ok: "Lliure per Volar",
        status_ok_sub: "Manté contacte visual.",
        status_warn: "Precaució",
        status_warn_sub: "NOTAMs actius a la vora.",
        status_rest: "Restringit",
        status_rest_sub: "Verifica permisos.",
        status_loading: "Analitzant...",
        search_placeholder: "Cerca lloc o zona...",
        checklist_title: "Llista de Seguretat",
        disclaimer_short: "Només informatiu. Consulta ROMATSA.",
        checklist: ["ID visible", "Bateria carregada", "Hèlixs OK", "Senyal provat", "Espai verificat", "Privacitat OK"],
        details_limit_low: "Límit Inferior",
        details_limit_up: "Límit Superior",
        details_status: "Estat",
        details_contact: "Contacte",
        details_notam_from: "Des de (UTC)",
        details_notam_to: "Fins a (UTC)",
        details_message: "Missatge"
    },
    ru: {
        status_ok: "Свободно для полетов",
        status_ok_sub: "Сохраняйте прямой визуальный контакт.",
        status_warn: "Требуется осторожность",
        status_warn_sub: "Рядом активные NOTAM.",
        status_rest: "Ограничено",
        status_rest_sub: "Проверьте разрешения.",
        status_loading: "Анализ...",
        search_placeholder: "Поиск места или зоны...",
        checklist_title: "Контрольный список",
        disclaimer_short: "Только для справки. Проверьте ROMATSA.",
        checklist: ["ID виден", "Батарея заряжена", "Винты в норме", "Сигнал проверен", "Воздух проверен", "Приватность"],
        details_limit_low: "Нижний предел",
        details_limit_up: "Верхний предел",
        details_status: "Статус",
        details_contact: "Контакт",
        details_notam_from: "От (UTC)",
        details_notam_to: "До (UTC)",
        details_message: "Сообщение"
    },
    zh: {
        status_ok: "可以飞行",
        status_ok_sub: "保持目视航线。",
        status_warn: "需要注意",
        status_warn_sub: "附近有活动的航行通告（NOTAM）。",
        status_rest: "限制飞行",
        status_rest_sub: "请检查所需的许可。",
        status_loading: "正在分析...",
        search_placeholder: "搜索地点或区域...",
        checklist_title: "预飞检查清单",
        disclaimer_short: "仅供参考。请确认 ROMATSA 官方信息。",
        checklist: ["ID可见", "电池已充电", "旋翼检查", "信号测试成功", "空域已核实", "尊重隐私"],
        details_limit_low: "下限",
        details_limit_up: "上限",
        details_status: "状态",
        details_contact: "联系方式",
        details_notam_from: "开始时间 (UTC)",
        details_notam_to: "结束时间 (UTC)",
        details_message: "信息"
    }
};

let currentLang = localStorage.getItem('uav_lang') || 'ro';

document.addEventListener('DOMContentLoaded', () => {
    // Initialize i18n
    updateLanguageUI();

    // Map Layers Definitions
    const baseLayers = {
        "Dark Matter": L.tileLayer('https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png', {
            attribution: '&copy; CARTO'
        }),
        "Satellite (Ortho)": L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}', {
            attribution: 'Tiles &copy; Esri'
        }),
        "Light Mode": L.tileLayer('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png', {
            attribution: '&copy; CARTO'
        })
    };

    // Map Initialization
    const map = L.map('map', { 
        zoomControl: false,
        layers: [baseLayers["Dark Matter"]] 
    }).setView([45.9432, 24.9668], 7);
    
    // Add Layer Control
    const layerSwitcher = L.control.layers(baseLayers, {}, { position: 'topright', collapsed: true }).addTo(map);

    let uavData = { static: [], dynamic: [] };
    let geoLayer = null;
    let userMarker = null;

    // Load Data
    const staticUrl = 'uav_zones.json';
    const dynamicUrl = 'https://flightplan.romatsa.ro/init/geoserver/ows?service=WFS&version=1.0.0&request=GetFeature&typeName=carto%3Arestrictii_notam_pt_uav&maxFeatures=5000&outputFormat=application%2Fjson';

    Promise.all([
        fetch(staticUrl).then(r => r.json()),
        fetch(dynamicUrl).then(r => r.json()).catch(() => ({ features: [] }))
    ]).then(([st, dy]) => {
        uavData.static = st.features;
        uavData.dynamic = dy.features;
        renderMap();
        populateList(uavData.static.concat(uavData.dynamic));
    });

    function renderMap() {
        if (geoLayer) map.removeLayer(geoLayer);

        const stLayer = L.geoJSON({ type: "FeatureCollection", features: uavData.static }, {
            style: { fillColor: '#ff4d4d', weight: 2, color: '#ff944d', fillOpacity: 0.25 },
            onEachFeature: setupFeature
        });

        const dyLayer = L.geoJSON({ type: "FeatureCollection", features: uavData.dynamic }, {
            style: { fillColor: '#ffcc00', weight: 3, color: '#ffee00', fillOpacity: 0.45, dashArray: '5, 8' },
            onEachFeature: setupFeature
        });

        geoLayer = L.featureGroup([stLayer, dyLayer]).addTo(map);
    }

    function setupFeature(f, l) {
        l.on('click', (e) => {
            showDetails(f.properties);
            map.fitBounds(l.getBounds(), { padding: [50, 50] });
        });
    }

    // UI Logic
    const langList = Object.keys(I18N);
    document.getElementById('lang-toggle').onclick = () => {
        let idx = langList.indexOf(currentLang);
        currentLang = langList[(idx + 1) % langList.length];
        localStorage.setItem('uav_lang', currentLang);
        updateLanguageUI();
        populateList(uavData.static.concat(uavData.dynamic));
    };

    function updateLanguageUI() {
        const t = I18N[currentLang];
        document.getElementById('current-lang').innerText = currentLang.toUpperCase();
        
        document.querySelectorAll('[data-i18n]').forEach(el => {
            const key = el.getAttribute('data-i18n');
            if (t[key]) el.innerText = t[key];
        });

        document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
            const key = el.getAttribute('data-i18n-placeholder');
            if (t[key]) el.placeholder = t[key];
        });

        // Update Checklist
        const list = document.getElementById('checklist-items');
        list.innerHTML = '';
        t.checklist.forEach((item, i) => {
            const li = document.createElement('li');
            li.className = 'check-item';
            li.innerHTML = `<div class="check-box"></div><span>${item}</span>`;
            li.onclick = () => li.classList.toggle('done');
            list.appendChild(li);
        });
    }

    function populateList(features) {
        const container = document.getElementById('zone-list');
        container.innerHTML = '';
        features.forEach(f => {
            const p = f.properties;
            const name = p.notam_series ? `NOTAM ${p.notam_series}` : (p.zone_id || 'UAV Zone');
            const card = document.createElement('div');
            card.className = 'zone-card';
            card.innerHTML = `<h3>${name}</h3><div class="meta"><span>${p.lower_lim || 'SFC'} - ${p.upper_lim || 'UNL'}</span><span>${p.status || ''}</span></div>`;
            card.onclick = () => showLayerByProp(p.notam_series ? 'notam_series' : 'id', p.notam_series || p.id);
            container.appendChild(card);
        });
    }

    function showLayerByProp(key, val) {
        if (!geoLayer) return;
        geoLayer.eachLayer(group => {
            group.eachLayer(layer => {
                if (layer.feature.properties[key] === val) {
                    showDetails(layer.feature.properties);
                    map.fitBounds(layer.getBounds(), { padding: [20, 20] });
                }
            });
        });
    }

    function showDetails(p) {
        const t = I18N[currentLang];
        const panel = document.getElementById('details-panel');
        const content = document.getElementById('details-content');
        const isDy = p.notam_series !== undefined;
        
        let html = `<h3>${isDy ? 'NOTAM ' + p.notam_series : (p.zone_id || 'Zone')}</h3><div style="display:grid; grid-template-columns:1fr 1fr; gap:10px; margin-top:10px; font-size:12px;">`;
        html += `<div><b>${t.details_limit_low}</b><br>${p.lower_lim || 'SFC'}</div>`;
        html += `<div><b>${t.details_limit_up}</b><br>${p.upper_lim || 'UNL'}</div>`;
        
        if (isDy) {
            html += `<div style="grid-column:span 2"><b>${t.details_notam_from}</b><br>${p.dfrom}</div>`;
            html += `<div style="grid-column:span 2"><b>${t.details_notam_to}</b><br>${p.dto}</div>`;
            html += `<div style="grid-column:span 2"><b>${t.details_message}</b><br><i style="opacity:0.8">${p.message}</i></div>`;
        } else {
            html += `<div><b>${t.details_status}</b><br>${p.status}</div>`;
            html += `<div style="grid-column:span 2"><b>${t.details_contact}</b><br>${p.contact || 'N/A'}</div>`;
        }
        html += `</div>`;
        
        content.innerHTML = html;
        panel.classList.add('active');
        updateSafetyStatus(isDy ? 'warn' : 'rest');
    }

    function updateSafetyStatus(type) {
        const t = I18N[currentLang];
        const dot = document.getElementById('status-dot');
        const card = document.getElementById('status-card');
        const title = card.querySelector('h2');
        const sub = card.querySelector('p');

        dot.className = `status-dot ${type === 'warn' ? 'yellow' : (type === 'rest' ? 'red' : 'green')}`;
        title.innerText = t[`status_${type}`];
        sub.innerText = t[`status_${type}_sub`];
    }

    // Geocoding Search
    const searchInput = document.getElementById('zone-search');
    let searchTimeout = null;

    searchInput.oninput = (e) => {
        clearTimeout(searchTimeout);
        const val = e.target.value;
        
        // Filter zones locally immediately
        const filteredZones = uavData.static.concat(uavData.dynamic).filter(f => {
            const p = f.properties;
            return (p.zone_id || '').toLowerCase().includes(val.toLowerCase()) || 
                   (p.notam_series || '').toLowerCase().includes(val.toLowerCase()) || 
                   (p.message || '').toLowerCase().includes(val.toLowerCase());
        });
        populateList(filteredZones);

        // Geocode after delay (Google Maps style)
        if (val.length > 3) {
            searchTimeout = setTimeout(() => {
                fetch(`https://nominatim.openstreetmap.org/search?format=json&q=${val}, Romania`)
                    .then(r => r.json())
                    .then(results => {
                        if (results.length > 0) {
                            const first = results[0];
                            map.setView([first.lat, first.lon], 12);
                            L.marker([first.lat, first.lon]).addTo(map).bindPopup(first.display_name).openPopup();
                        }
                    });
            }, 800);
        }
    };

    // Modal Control
    document.getElementById('safety-toggle').onclick = () => document.getElementById('safety-modal').style.display = 'flex';
    document.querySelector('.close-modal').onclick = () => document.getElementById('safety-modal').style.display = 'none';
    document.querySelector('.close-details').onclick = () => document.getElementById('details-panel').classList.remove('active');

    // Geoloc
    document.getElementById('locate-me').onclick = () => {
        map.locate({setView: true, maxZoom: 14});
    };
    
    map.on('locationfound', (e) => {
        if (userMarker) map.removeLayer(userMarker);
        userMarker = L.circleMarker(e.latlng, { radius: 8, color: '#7bd0ff', fillColor: '#7bd0ff', fillOpacity: 0.8 }).addTo(map);
        updateSafetyStatus('ok');
    });
});
